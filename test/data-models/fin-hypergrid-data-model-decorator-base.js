/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-data-model-decorator-base');

describe('<fin-hypergrid-data-model-decorator-base>', function() {

    describe('data-models/fin-hypergrid-data-model-decorator-base.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
