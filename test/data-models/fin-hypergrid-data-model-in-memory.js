/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-data-model-in-memory');

describe('<fin-hypergrid-data-model-in-memory>', function() {

    describe('data-models/fin-hypergrid-data-model-in-memory.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
