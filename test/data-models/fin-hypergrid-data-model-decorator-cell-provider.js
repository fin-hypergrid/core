/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-data-model-decorator-cell-provider');

describe('<fin-hypergrid-data-model-decorator-cell-provider>', function() {

    describe('data-models/fin-hypergrid-data-model-decorator-cell-provider.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
