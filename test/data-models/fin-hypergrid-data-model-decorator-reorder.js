/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-data-model-decorator-reorder');

describe('<fin-hypergrid-data-model-decorator-reorder>', function() {

    describe('data-models/fin-hypergrid-data-model-decorator-reorder.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
