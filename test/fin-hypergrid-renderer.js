/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-renderer');

describe('<fin-hypergrid-renderer>', function() {

    describe('fin-hypergrid-renderer.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
