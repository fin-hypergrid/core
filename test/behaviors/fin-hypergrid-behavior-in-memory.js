/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-behavior-in-memory');

describe('<fin-hypergrid-behavior-in-memory>', function() {

    describe('behaviors/fin-hypergrid-behavior-in-memory.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
