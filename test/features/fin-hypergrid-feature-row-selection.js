/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-row-selection');

describe('<fin-hypergrid-feature-row-selection>', function() {

    describe('features/fin-hypergrid-feature-row-selection.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
