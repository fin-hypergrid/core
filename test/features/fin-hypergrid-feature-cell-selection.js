/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-cell-selection');

describe('<fin-hypergrid-feature-cell-selection>', function() {

    describe('features/fin-hypergrid-feature-cell-selection.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
