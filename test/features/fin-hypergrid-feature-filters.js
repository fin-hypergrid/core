/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-filters');

describe('<fin-hypergrid-feature-filters>', function() {

    describe('features/fin-hypergrid-feature-filters.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
