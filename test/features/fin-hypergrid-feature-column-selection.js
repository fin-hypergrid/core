/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-column-selection');

describe('<fin-hypergrid-feature-column-selection>', function() {

    describe('features/fin-hypergrid-feature-column-selection.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
