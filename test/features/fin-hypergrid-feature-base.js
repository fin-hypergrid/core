/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-base');

describe('<fin-hypergrid-feature-base>', function() {

    describe('features/fin-hypergrid-feature-base.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
