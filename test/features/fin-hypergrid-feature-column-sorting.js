/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-column-sorting');

describe('<fin-hypergrid-feature-column-sorting>', function() {

    describe('features/fin-hypergrid-feature-column-sorting.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
