/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-cell-editing');

describe('<fin-hypergrid-feature-cell-editing>', function() {

    describe('features/fin-hypergrid-feature-cell-editing.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
