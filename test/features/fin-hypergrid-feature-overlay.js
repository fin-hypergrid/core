/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-overlay');

describe('<fin-hypergrid-feature-overlay>', function() {

    describe('features/fin-hypergrid-feature-overlay.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
