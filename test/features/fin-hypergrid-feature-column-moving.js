/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-column-moving');

describe('<fin-hypergrid-feature-column-moving>', function() {

    describe('features/fin-hypergrid-feature-column-moving.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
