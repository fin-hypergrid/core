/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-feature-key-paging');

describe('<fin-hypergrid-feature-key-paging>', function() {

    describe('features/fin-hypergrid-feature-key-paging.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
