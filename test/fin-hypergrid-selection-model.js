/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-selection-model');

describe('<fin-hypergrid-selection-model>', function() {

    describe('fin-hypergrid-selection-model.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
