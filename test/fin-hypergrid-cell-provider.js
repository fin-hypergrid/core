/* globals describe, it, assert */

'use strict';

var customElement = document.querySelector('fin-hypergrid-cell-provider');

describe('<fin-hypergrid-cell-provider>', function() {

    describe('fin-hypergrid-cell-provider.js', function() {
        it('should have real tests filled out', function() {
            assert.equal(customElement, customElement);
        });

    });

});
